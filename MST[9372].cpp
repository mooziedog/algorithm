#include <stdio.h>
#include <vector>
#include <algorithm>

using namespace std;

int T;
int main() {
	scanf("%d", &T);

	for (int t = 1; t <= T; t++) {
		int N, M;
		scanf("%d%d", &N, &M);

		while (M--) {
			int a, b;
			scanf("%d%d", &a, &b);
		}

		printf("%d\n", N - 1);
	}
}